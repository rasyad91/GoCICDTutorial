name: go setup

on: 
  push:
    branches: [main]

jobs:
  my-job:
    name: My job
    runs-on: ubuntu-latest
    steps:
    - name: Print a greeting
      run: |
        echo Hello world
        ls -lAhFa
